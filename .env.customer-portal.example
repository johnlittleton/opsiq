# Cloudflare Pages customer website build settings
VITE_CUSTOMER_PORTAL_ONLY=true
# Use the deployed customer API or same-origin proxy URL. Do not use localhost.
VITE_API_BASE=https://YOUR_CUSTOMER_API_HOST
