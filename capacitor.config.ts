import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.slingshot.opsiq.mobile',
  appName: 'OpsIQ Mobile',
  webDir: 'dist/renderer'
};

export default config;
